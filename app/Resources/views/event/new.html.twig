{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="page-header">Event creation</h1>
<div class="container">
    <div class="row">
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn btn-default" type="submit" value="Create" />
    {{ form_end(form) }}


            <a href="{{ path('event_index') }}" class="btn btn-default">Back to the list</a>
</div>
</div>
{% endblock %}
